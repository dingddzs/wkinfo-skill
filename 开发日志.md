# 开发日志

## 2026-07-16 阶段 1：环境与登录态

**目标**：打通"启动浏览器 → 注入 cookie → 登录威科"链路

### 关键发现

- 用户的 Edge 默认 profile 有 20 个 msedge.exe 进程，无法再启 debug 实例（profile 锁定）
- 解决方案：用 Playwright `connect_over_cdp` + **隔离 profile**（`D:/ai/Claudecode/.../edge-debug-profile/`），启动一个独立 Edge 实例
- Python Playwright 可用，Node 版本不可用 → 全部 Python 实现
- Windows 上 `subprocess.Popen` 启 Edge 失败（profile 锁定），改用 PowerShell `Start-Process` 解决

### 产出

- `install_cookies.py` — 启动隔离 Edge + 注入 17 个 cookie + 验证登录态
- 默认 `--no-kill`，不杀现有 Edge；`--kill` 参数为破坏性

### 验证

```
[OK] 已登录 (jtnfawkwechat)
```

---

## 2026-07-16 阶段 2：威科案例页调研

**目标**：摸清搜索页结构、API 端点、下载流程

### 关键发现

#### URL 路径
- ✅ **正确**：`/judgment-documents/list` — 裁判文书搜索页
- ❌ 错误：`/cases/list`（老 skill 文档提到的，已 404）
- ⚠️ 区分：`/case-analysis/list` 是案例评析（评论文章），不是判决文书

#### 搜索 API（关键！）
- `POST /csi/search` — 主搜索
- `POST /csi/search/doc-count` — 计数
- **必填字段**：`indexId: "law.case"`（否则 400）
- **queryString 格式**：`simple:((query))` — 必须用此格式包，普通文本无效
- **排序**：`score` DESC 优先（否则返回 score=0 的按日期排序数据）
- **filterQueries 语法**：`+field:((value))` 单值；OR 用 `+field:((1 OR 2 OR 3))`
- **文档实际匹配数**：在响应 `searchMetadata.docCount`（不是 `data.total`）
- **其他有效字段**：
  - `courtLevel: ((1))` — 法院级别（1=最高, 2=高院, 3=中院, 4=基层, 5=专门）
  - `typeOfDecisionCode: ((001))` — 文书类型（001=判决, 002=裁定, 003=决定, 004=调解, 006=通知书, 008=案例评析）
  - `judgmentDate: ([2021 TO 2025])` — Lucene 范围语法（待进一步验证）

#### 下载 API（核心发现：3 步流程）
```
1. POST /csi/document/downloadLimit  → 检查权限 (返回 {result: true/false})
2. POST /csi/document/downloadPath   → 获取 download key (UUID)
3. GET  /api/download?key={uuid}    → 实际下载文件
```

- fileType 值：`pdf` / `docx` / `excel`（注意 xls 要写 `excel`）
- Excel/Word 需要 `cellList` 字段（24 个英文逗号分隔的字段名）
- 文件名在 `Content-Disposition` 的 `filename*=UTF-8''...` 部分（`filename=` 是 Latin-1 会乱码）
- 验证：直接用 `requests` 调用完整流程，无需浏览器

#### 详情页工具栏（浏览器场景备用）
- `#excel{caseId}` / `#word{caseId}` / `#pdf{caseId}` / `#print{caseId}` / `#favorites{caseId}` / `#email{caseId}`
- 点击会弹 modal 选"清洁版本/带目录版本/含链接"，点"下载"才真正触发 API

### 产出

- `references/wkinfo-cases-page.md` — 完整调研文档
- 12 张截图保存在 `临时资源/截图/`
- 12 个调研脚本保留在 `脚本和代码/_research/`

### 遇到的问题

1. **uid 提取失败**：cookie value 是 URL-encoded，regex 没解码 → 改用 `urllib.parse.unquote()` 后 OK
2. **doc-count 400**：缺 `indexId` 字段 → 加上后 200
3. **搜索返回 score=0 的结果**：queryString 没用 `simple:()` 格式 + sort 没按 score → 修复后返回真实匹配

---

## 2026-07-16 阶段 3：自然语言解析器

**目标**：从用户原话中抽取案由、时间、法院、审级、文书类型

### 关键发现

#### 案由词典：基于《民事案件案由规定》(2025)
- 来源：用户提供的 `.doc` 文件（`D:\download\最高人民法院关于印发修改后的《民事案件案由规定》的通知（2025）_2026.01.01生效_20260716下载.doc`）
- 用 antiword 提取文本 → 正则解析 → JSON
- **修复的关键 bug**：
  - 跳过目录部分（检测"规定如下："标记）
  - Level 2 数字和顿号间允许空格（`十 、合同纠纷`）
- **正确数量**：12 Part / 59 Category / 514 Cause / 470 Sub-cause ✓ 与文档官方数字吻合

#### 解析器架构
- `CaseCauseDict`：倒排索引，每个案由和别名建索引
- 匹配分数：基础 1.0 + 别名 0.9 + 关键词长度加成 * 0.01
- 典型匹配："建设工程施工合同纠纷" → 第四级 (3) 建设工程施工合同纠纷，score=2.18

### 已知限制

- 用户场景描述（如"实际施工人向发包人请求付款"）不直接命中案由名
  - **缓解**：原始 query 直接传给威科 `queryString`，让 wkinfo 的 smart matching 处理

### 产出

- `处理后文件/民事案由_2025.json` — 结构化案由词典
- `references/nl-patterns.md` — 抽取规则文档

---

## 2026-07-16 阶段 4：搜索 CLI 与渐进式逼近

**目标**：实现结构化 CLI，含 list 模式渐进式逼近 ≤200 条

### 渐进式逼近策略
1. 仅关键词
2. + 文书类型=判决书（`typeOfDecisionCode:((001))`）
3. + 中级法院及以上（`courtLevel:((1 OR 2 OR 3))`）
4. 截取前 N 条（按 score DESC + judgmentDate DESC 排序）

### 验证
```
[Stage 0] 仅关键词: 建设工程施工合同纠纷 → 2,487,461
[Stage 1] + 判决书 → 1,010,644
[Stage 2] + 中级以上法院 → 346,141
[Stage 3] 截取前 10 条 → 共 10 条
```

### 产出

- `search_cases.py` — CLI 入口

---

## 2026-07-16 阶段 5：下载 CLI + PDF 标黄

**目标**：实现 PDF/DOCX/XLS 三种格式下载 + PDF 标黄

### 关键发现
- `xls` 格式在 API 里叫 `excel`
- Excel/Word 需要 `cellList` 字段（24 个字段名）
- 默认 cellList：`title,documentnumber,court,territory,typeofcase,causeofaction,judgmentdatestr,instance,typeofdecision,subjectFee,courtAcceptanceFee,judgmentReason,judgmentResult,judges,plaintiff,defendant,thirdParty,trialProcess,plaintiffattorney,defendantattorney,thirdpartyattorney,plaintiffagent,defendantagent,thirdpartyagent`

### 验证
- few 模式下载 3 个 PDF：成功 3/3
- list 模式下载 5 个 Excel：成功 5/5
- PDF 标黄：成功 3/3（pypdf 5.0+ 兼容性已处理）

### 产出
- `download.py` — 下载 CLI
- `highlight_pdf.py` — PDF 标黄（黄色便签注释）

---

## 2026-07-16 阶段 6：集成测试

端到端验证 few / list / xls / pdf 各种组合：
- ✅ 检索 → 搜索结果 JSON → 下载 PDF
- ✅ 检索 → 渐进式逼近 → 截取前 N 条 → 下载 Excel
- ✅ Cookie 共享：登录态验证通过
- ✅ 案由匹配：建设工程相关案由识别正确

---

## 2026-07-16 阶段 7：打包成 Skill

将 `脚本和代码/` + `references/` 拷贝到 `~/.claude/skills/威科案例检索和下载/`，写 SKILL.md frontmatter。

待完成：
- [x] 编写 SKILL.md
- [x] 拷贝到 ~/.claude/skills/
- [ ] 触发准确率测试